# Backend (Spring) — default matches local backend
PUBLIC_API_URL=http://localhost:8080

# This Astro app URL (Discord Developer Portal → OAuth2 redirect must match …/auth/callback)
PUBLIC_SITE_ORIGIN=http://localhost:4321

# Discord application → OAuth2 → Client ID (same as in backend / bot)
PUBLIC_DISCORD_CLIENT_ID=
